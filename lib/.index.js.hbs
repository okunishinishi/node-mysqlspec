/**
 * {{pkg.description}}
 * @module {{pkg.name}}
 * @version {{pkg.version}}
 * @author {{pkg.author.name}}
 * @license {{pkg.license}}
 */

"use strict";

/** @lends module:{pkg.name}} */
var {{pkg.name}} = require('./{{pkg.name}}');

module.exports = {{pkg.name}};